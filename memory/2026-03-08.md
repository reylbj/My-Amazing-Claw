# 2026-03-08

- 症状：聊天链路反复出现 `Unknown system error -11, read`，并触发 gateway fallback，响应变慢。
- 根因：`agents.defaults.workspace` 指向 Desktop 中文/emoji 路径（文件提供层不稳定），读写偶发失败。
- 固化：workspace 切换为 `~/.openclaw/workspace-runtime-real`（真实本地目录），保留最小运行副本。
- 验证：`openclaw gateway status` 为 `RPC probe: ok`；回归 3 轮读写均 `OK`；`gateway.err.log` 无新增 `-11`。
