- 2026-03-15：完成 OpenClaw 安全加固。最终策略为：WhatsApp/Telegram/企业微信群 `groupPolicy=disabled`（不是空 `allowlist`），企业微信私聊 `dmPolicy=allowlist` 且 `allowFrom=["yuyirui"]`，`tools.elevated.allowFrom` 显式收口到 WhatsApp `+447942898789` / WeCom `yuyirui`，并隔离 `~/.openclaw/skills/openclawmp` 为 disabled 目录。原因：OpenClaw 2026.3.13 对开放型 provider 存在兼容逻辑——若 `groupPolicy=allowlist` 但没有任何 `groupAllowFrom/groups`，运行态会回退按 `open` 处理；要实现“默认彻底关闭群”，必须用 `disabled`，不能用空 allowlist。
- 2026-03-15：安全审计加固后，外部攻击面结论为 `groups: open=0, allowlist=0`；剩余 deep audit critical 主要是静态代码扫描类：`wecom-openclaw-plugin`、`wechat-article-formatter`、以及已隔离的 `openclawmp`。判断：前两者属业务在用的高能力组件，可保留但按受控组件看待；`openclawmp` 继续隔离。
- 2026-03-15：Control UI 若出现“100% context used”不一定是真打满。此次排查确认主会话 `agent:main:main` 实际约为 `166k/272k ≈ 61%`；前端 100% 更可能是旧 session / 缓存 / 显示层映射问题，不应直接按“上下文已满”处理。
